<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vutuan.orderfood">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DangNhapActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DangKyActivity" />
        <activity android:name=".TrangChuActivity" />
        <activity
            android:name=".ThemBanAnActivity"
            android:label="@string/themBanAn"
            android:theme="@style/PopUpActivity" />
        <activity
            android:name=".ThemLoaiMonAnActivity"
            android:label="@string/themLoaiMonAn"
            android:theme="@style/PopUpActivity" />
        <activity android:name=".ThemThucDonActivity"
            android:label="@string/themThucDon"></activity>
        <activity android:name=".ThemSoLuongActivity"
            android:label="@string/themSoLuong"
            android:theme="@style/PopUpActivity"></activity>
    </application>

</manifest>